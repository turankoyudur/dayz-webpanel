export declare class PacketAssembler {
    private assemblyMap;
    private readonly timeoutMs;
    constructor(timeoutMs?: number);
    handlePart(body: Buffer): Buffer | null;
    private isComplete;
    private assemble;
    private cleanup;
    clear(): void;
}
