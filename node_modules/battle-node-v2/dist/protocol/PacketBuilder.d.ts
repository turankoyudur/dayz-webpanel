export declare class PacketBuilder {
    static buildLogin(password: string): Buffer;
    static buildCommand(sequence: number, command: string): Buffer;
    static buildKeepAlive(sequence: number): Buffer;
    static buildAcknowledge(sequence: number): Buffer;
    private static constructPacket;
}
