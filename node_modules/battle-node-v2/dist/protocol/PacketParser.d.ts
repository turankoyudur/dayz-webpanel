import { PacketType } from './Constants.js';
export type PacketParseResult = {
    isValid: false;
    error: 'too_short' | 'invalid_header' | 'crc_mismatch' | 'invalid_marker';
} | {
    isValid: true;
    type: PacketType;
    body: Buffer;
};
export interface MultipartHeader {
    sequence: number;
    total: number;
    index: number;
    data: Buffer;
}
export declare class PacketParser {
    static parse(buffer: Buffer): PacketParseResult;
    static isMultipart(body: Buffer): boolean;
    static parseMultipartHeader(body: Buffer): MultipartHeader;
}
